import 'package:flutter/material.dart';

class FavoritesMealsScreen extends StatelessWidget {
  const FavoritesMealsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Favorites"),
    );
  }
}
